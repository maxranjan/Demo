public class Array1 {
    public static void main(String[] args) {
        // Array Declaration
        // int[] a;
        // Array creation.
        // a = new int[3];

        // Array creation and Declaratoion in one line
        // int[] a = new int[0]; <We can create an array whose size is 0>
        // int[] a = new int[-5]; <We Cannot put the size of array is negative.>4

        // int[] a = { 10, 20, 30 };
        // for (int i = 0; i < a.length; i++) {
        //     System.out.println(a[i]);
        // }

        //  For Each Loop 
        int[] a = { 10, 20, 30 };
        for(int x:a)
        {
            System.out.println(x);
        }

    }
}
